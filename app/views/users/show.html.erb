<body>
<main>
    <div class="container px-5 px-sm-0">
    <%= render 'users/error', book: @book %>
      <div class="row">
        <div class="col-md-3">
          <h2>User info</h2>
          <div class="attachment user profile_image fallback"><%= attachment_image_tag @user, :profile_image, :fill, 50, 50, fallback: "no_image.jpg", size:'100x100' %></div>
            <table class="table">
              <tbody>
                <tr></tr>
                <tr>
	                <th>name</th>
	                <th><%= @user.name %></th>
                </tr>
                <tr>
                  <th>introduction</th>
                  <th><%= @user.introduction %></th>
                </tr>
              </tbody>
            </table>

          <div class="row">
            <a class="btn btn-outline-secondary btn-block fas fa-user-cog edit_user_17488" href="<%= edit_user_path(current_user.id) %>"></a>
          </div>

           <h2 class="mt-3">New book</h2>
<!-------------------------------------------------------------------------------------------->
      <%= render 'books/input_newbook', book: @book %>
<!-------------------------------------------------------------------------------------------->
          </div>

        <div class="col-md-8 offset-md-1">
            <h2>Books</h2>
              <table class="table table-hover table-inverse mb-0">

              <thead>
                <tr>
                  <th></th>
                  <th></th>
                  <th></th>
                    <th>Title</th>
                    <th></th>
                    <th>Opinion</th>
                    <th colspan="3"></th>
                </tr>
              </thead>

                <table class="table">
    		          <tbody>
    		            <tr>
    		              <% @books.each do |book| %>
                      <td><a href="">
                        <%= attachment_image_tag @user, :profile_image, :fill, 40, 40, fallback: "no_image.jpg", size:'40x40' %><br>
                          </a>
                      </td>
                      <td>
                        <%= link_to book_path(book.id) do %>
                          <% book.title %>
                        <% end %>
                      </td>
                      <td><%= book.body %></td>
                      </tr>
                      </tbody>
                  <% end %>
                </table>
                <tbody>
              </tbody>
            </table>
        </div>
      </div>
    </div>
  </main>
</body>